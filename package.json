{
  "$schema": "http://json.schemastore.org/package",
  "private": true,
  "name": "u2a",
  "version": "0.0.0",
  "description": "Lossy Unicode -> Ascii converter. Based on the python Unidecode package.",
  "main": "dist/bundle.js",
  "dependencies": {},
  "devDependencies": {
    "ava": "^0.19.1",
    "nyc": "^10.3.2",
    "rollup": "^0.50.0",
    "tslint": "^5.3.2",
    "tslint-language-service": "^0.9.6",
    "typescript": "^2.3.3"
  },
  "scripts": {
    "prebuild": "tsc -p tsconfig.prebuild.json && node prebuild/generate_data_from_py.js",
    "build": "tsc -p tsconfig.build.json",
    "postbuild": "rollup build/index.js --output.name unidecode --output.format iife --output.file dist/bundle.js",
    "pretest": "tsc -p tsconfig.build.json",
    "test": "nyc --reporter=lcov ava build/test.js",
    "posttest": "nyc report"
  },
  "author": "Chacha-26 <chacha-26@protonmail.com>",
  "license": "ISC"
}
